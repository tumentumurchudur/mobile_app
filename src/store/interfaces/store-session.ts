export interface IStoreSession {
  /**
   * The session storage key to be used to store implemented state slice in session.
   *
   * @type {string}
   * @memberof to IStoreSession
   */
  sessionStorageKey: string;
}
